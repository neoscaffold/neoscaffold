<div class="nbr">
  <div class="wf">
    {{#if this.canBackPage}}
      {{!-- template-lint-disable require-presentational-children --}}
      <div class="bk" role="button" {{on "click" this.goBack}}>
        <i class="fa-solid fa-angle-left fa" aria-hidden="true"></i>
      </div>
    {{else}}
      {{#if @linkUrl}}
        {{#if @linkParam}}
          <LinkTo @route={{@linkUrl}} @model={{@linkParam}} class="bk" role="button">
            <i class="fa-solid fa-angle-left fa" aria-hidden="true"></i>
          </LinkTo>
        {{else}}
          <LinkTo @route={{@linkUrl}} class="bk" role="button">
            <i class="fa-solid fa-angle-left fa" aria-hidden="true"></i>
          </LinkTo>
        {{/if}}
      {{/if}}
    {{/if}}
    {{yield}}
    {{!-- template-lint-disable require-presentational-children --}}
    <LinkTo @route="directory" class="menul" role="button">
      <nav aria-label="menu">
        <i class="fa-solid fa-bars fa" aria-hidden="true"></i>
      </nav>
    </LinkTo>
  </div>
</div>
